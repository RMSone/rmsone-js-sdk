div(ng-include='"components/navbar/navbar.html"')
.page-col-center
  .banner
    h2.hidden-small Change Password
  .account-container
      .row 
        .margin-botttom     
        .col-sm-12
          form.form(name='form', ng-submit='changePassword(form)', novalidate='')
            .form-group
              input.form-control(type='password', name='password', ng-model='user.oldPassword', mongoose-error='',placeholder='Current Password')
              p.help-block(ng-show='form.password.$error.mongoose')
                | {{ errors.other }}
            .form-group
              input.form-control(type='password', name='newPassword', ng-model='user.newPassword', ng-minlength='3', required='',placeholder='New Password')
              p.help-block(ng-show='(form.newPassword.$error.minlength || form.newPassword.$error.required) && (form.newPassword.$dirty || submitted)')
                | Password must be at least 3 characters.

            p.help-block  {{ message }}

            button.redButton(type='submit') Save changes
