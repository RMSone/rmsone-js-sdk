.rms-logo
.page-col-center
  .banner
    h2.hidden-small Sign in to continue to RMS(one)
  .account-container
    .row
      .col-sm-12
        img.profile-img(ng-src='{{userpic}}')
      .col-sm-12
        form.form(name='form', ng-submit='login(form)', novalidate='')
          .form-group
              input.form-control(type='text', name='email', ng-model='user.email',placeholder='Email')
          .form-group
            input.form-control(type='password', name='password', ng-model='user.password',placeholder='Password')
          .form-group.has-error
            p.help-block(ng-show='form.email.$error.required && form.password.$error.required && submitted')
            | Please enter your email and password.
            p.help-block {{ errors.other }}
          div
            button.redButton(type='submit') Login
          //-div.loginoption
            span.remember
              input(name='PersistentCookie', type='checkbox', value='yes', checked='checked')
              span
                | Stay signed in
            a.need-help-reverse(href='/signup') Need help?

  .register
    a(href='/signup') Create a account
  //.loginbtmmsg
  //  | RMS &#40;one&#41: Open, Real-Time Risk Management
           
           

